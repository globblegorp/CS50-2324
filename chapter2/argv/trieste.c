/*
 *Read through the code below.
 *Without changing the code, run the program with the correct
 *command line arguments, so that it prints the word
 *"Trieste" (a scenic port city on the Adriatic coast).
 */
#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(int argc, string argv[])
{
    printf("%c", argv[1][0]);
    printf("%c", argv[4][3]);
    printf("%c", argv[2][2]);
    printf("%c", argv[3][5]);
    printf("%c", argv[4][0]);
    printf("%c", argv[2][0]);
    printf("%c", argv[1][6]);
    printf("\n");
}
