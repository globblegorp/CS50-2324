#include <cs50.h>
#include <stdio.h>

int main(void)
{
    // Prompt the user for the height, it should be between 1 and 8

    // Print the triangle
    // For each row
    for()
        // Print the number of spaces
        for()

        // Print the number of #
        for()
}