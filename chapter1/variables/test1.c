/**
 * This program will create variables, assign values, and
 * print them out.
 *
 * Brandon Price
 * CS50 AP
 * Mount Greylock
*/
#include <stdio.h>

int main(void)
{
    // Declare an integer variable called x and assign the value
    // 7 to it
    int x = 7;

    // Declare an integer variable called y and assign the value
    // 6 to it
    int y = 6;

    // Print out the current value of x and y
    printf("The value of x is %i and the value of y is %i\n", x, y);
}