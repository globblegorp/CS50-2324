// Demonstrates an infinite loop
#include <cs50.h>
#include <stdio.h>

int main(void)
{
    // This is an infinite loop that we can stop in the terminal
    // with ctr + c
    while(true)
    {
        printf("hello, world\n");
    }
}